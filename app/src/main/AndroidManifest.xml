<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rpll.okeoke.bettingplatform">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".View.MainActivity" />
        <activity
            android:name=".View.RegisterActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".View.FirstActivity" />
        <activity
            android:name=".View.LoginActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity
            android:name=".View.ChatActivity"
            android:windowSoftInputMode="adjustPan" />
        <activity android:name=".View.LoadingActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".View.HomeActivity"
            android:label="@string/title_activity_home" />
        <activity android:name=".View.TopUpActivity" />
        <activity android:name=".View.EditProfileActivity" />
        <!-- <activity android:name=".View.popup_topup"></activity> -->
        <activity android:name=".View.WithdrawActivity" />
        <activity android:name=".View.BetDetailActivity" />
        <activity android:name=".View.AboutActivity" />
        <activity android:name=".View.ReportActivity" />
        <activity android:name=".View.HelpActivity" />
        <activity android:name=".View.ViewReportActivity" />
        <activity android:name=".View.ViewDetailReportActivity"></activity>
    </application>

</manifest>